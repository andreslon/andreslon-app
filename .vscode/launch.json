{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/andreslon-app/package.json",
                "localRoot": "${workspaceFolder}/andreslon-app"
            }
        }
    ]
}